<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
                 x:Class="Grocery.App.Views.RegisterView"
             x:DataType="vm:RegisterViewModel"
             Title="RegisterView">
    <StackLayout Padding="20" VerticalOptions="Center">
        <Label Text="Register" FontSize="24" HorizontalOptions="Center" />
        <Entry Placeholder="first name" Text="{Binding FirstName}" />
        <Entry Placeholder="last name" Text="{Binding LastName}" />
        <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" />
        <Entry Placeholder="Password" Text="{Binding Password}" IsPassword="True" />
        <Entry Placeholder="verify Password" Text="{Binding VerifyPassword}" IsPassword="True" />
        <Button Text="Register" Command="{Binding RegisterCommand}" />
        <Label Text="{Binding RegisterMessage}" TextColor="Red" />
    </StackLayout>
</ContentPage>